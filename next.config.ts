import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable static export for Chrome extension
  // output: 'export', // Uncomment when building extension
};

export default nextConfig;
